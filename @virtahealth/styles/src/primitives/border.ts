/**
 * border values
 * @module border
 */

// Disable this because if we don't specify number then down the line we end up getting
// TS angry because we have types that are specific numbers that don't mix well
/* eslint-disable @typescript-eslint/no-inferrable-types */
export const borderRadius1: number = 4;
export const borderRadius2: number = 12;
export const borderRadius3: number = 20;

export const borderWidth0: number = 0;
export const borderWidth1: number = 1;
export const borderWidth2: number = 2;
export const borderWidth3: number = 3;
export const borderWidth4: number = 4;
/* eslint-enable @typescript-eslint/no-inferrable-types */
