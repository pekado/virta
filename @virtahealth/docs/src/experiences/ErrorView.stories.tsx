import * as React from "react";
import { ErrorView } from "@virtahealth/experiences";

export default {
  title: "Experiences / Error View",
  component: ErrorView,
};

export const Example = () => <ErrorView />;
