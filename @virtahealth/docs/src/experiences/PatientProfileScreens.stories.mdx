import { action } from "@storybook/addon-actions";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {
  PreferredNameScreen,
  NameScreen,
  EmailScreen,
  PhoneScreen,
  PersonalGoalScreen,
  EmergencyContactScreen,
  TimeZoneScreen,
  LanguageScreen,
} from "@virtahealth/experiences";

<Meta title="Experiences / PatientProfileScreens" />

# PatientProfileScreens

`PatientProfileScreens` are screens that include 0-3 inputs and a save button. Some of these, such as phone and timezone, only accept specific input.

<Preview>
  <Story name="Name">
    <View>
      <NameScreen value="My name" changeValue={() => {}} disableSave={false} />
    </View>
  </Story>
  <Story name="Preferred Name">
    <View>
      <PreferredNameScreen
        value="My preferred name"
        changeValue={() => {}}
        disableSave={false}
      />
    </View>
  </Story>
  <Story name="Email">
    <View>
      <EmailScreen
        value="email@email.com"
        changeValue={() => {}}
        disableSave={false}
      />
    </View>
  </Story>
  <Story name="Phone">
    <View>
      <PhoneScreen value="" changeValue={() => {}} disableSave={false} />
    </View>
  </Story>
  <Story name="Personal Goal">
    <View>
      <PersonalGoalScreen value="" changeValue={() => {}} disableSave={false} />
    </View>
  </Story>
  <Story name="Emergency Contact">
    <View>
      <EmergencyContactScreen
        value={{ name: "", phone: "", relationship: "" }}
        changeValue={() => {}}
        disableSave={false}
      />
    </View>
  </Story>
  <Story name="Timezone">
    <View>
      <TimeZoneScreen value="" changeValue={() => {}} disableSave={false} />
    </View>
  </Story>
  <Story name="Language">
    <View>
      <LanguageScreen
        value=""
        changeValue={() => {}}
        onUnsupportedLanguage={() => {}}
        disableSave={false}
      />
    </View>
  </Story>
</Preview>

## API Reference

<Props of={PreferredNameScreen} />
