import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { FormElementLabel } from "@virtahealth/components";

<Meta title="Components / FormElementLabel" component={FormElementLabel} />

# FormElementLabel

<Preview>
  <Story name="label only">
    <FormElementLabel
      labelMessage={{ id: "components.formElementLabel.label" }}
    />
  </Story>
  <Story name="helper text">
    <FormElementLabel
      labelMessage={{ id: "components.formElementLabel.label" }}
      helperText={{ id: "components.formElementLabel.helperText" }}
    />
  </Story>
</Preview>

## API Reference

<Props of={FormElementLabel} />
