import { action } from "@storybook/addon-actions";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { Body } from "@virtahealth/components";

<Meta title="Components / AutoComplete" />

# AutoComplete

`AutoComplete` is a text input that accepts a list of suggestions to help the user auto complete their text.

<Preview>
  <Story name="AutoComplete">
    <Body>See Experiences / PatientProfileScreens / Timezone</Body>
  </Story>
</Preview>

## API Reference

<Props of={AutoComplete} />
