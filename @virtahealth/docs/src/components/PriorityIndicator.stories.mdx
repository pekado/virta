import { PriorityIndicator } from "@virtahealth/components";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";

<Meta title="Components / PriorityIndicator" component={PriorityIndicator} />

# PriorityIndicator

`PriorityIndicator` is a used to call attentiont to an important field that may need attention

<Preview>
  <Story name="default">
    <PriorityIndicator color="red" />
    <PriorityIndicator color="orange" />
    <PriorityIndicator color="green" />
  </Story>
</Preview>

## API Reference

<Props of={PriorityIndicator} />
